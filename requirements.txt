# Runtime dependencies — what the API and engine need to run.
# Pinned exactly (==) so a rebuild in six months installs what works today.
anthropic==1.0.0
fastapi==0.141.1
pydantic==2.13.4
python-dotenv==1.2.3
PyYAML==6.0.3
uvicorn==0.52.4
